#ifndef UI_PARSER_H
#define UI_PARSER_H

#include "../ui_core.h"

// Loads a UI Asset (Memory + Spec Tree) from a YAML file.
// UiAsset* ui_parser_load_from_file(const char* path);

#endif // UI_PARSER_H
